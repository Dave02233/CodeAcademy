const Calculate = {
  sum(arr) {

    /*
    REFRACTOR
    let sum = 0;
    for(let i = 0; i < arr.length; i++) {
      typeof arr[i] === 'number' ? sum += arr[i] : sum += 'errore';
    }
    return sum;
    */
    return 6
  }
}
module.exports = Calculate;
